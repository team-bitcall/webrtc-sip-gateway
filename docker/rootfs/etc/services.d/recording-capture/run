#!/command/with-contenv sh
set -eu
if [ "${SEAT_MODE:-disabled}" != managed ] || [ "${SEAT_RECORDING_ENABLED:-0}" != 1 ]; then
  exec sleep infinity
fi
exec python3 /opt/bitcall/seat/recording_runtime.py
